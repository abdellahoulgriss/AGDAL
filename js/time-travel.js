let currentTimeMode = 'present';const dialUpAudioContext = new (window.AudioContext || window.webkitAudioContext)();function travelToPast() {    if (currentTimeMode === 'past') return;        showTimeTransition('past');    setTimeout(() => {        currentTimeMode = 'past';        const body = document.body;                body.classList.remove('future-mode');        removeFutureElements();                body.classList.add('nineties-mode');                addCRTEffect();                addWindowsTaskbar();                transformCardsToNineties();                transformOwlToMouse();                updateTimeButtons('past');                playDialUpSound();    }, 500);}function travelToFuture() {    if (currentTimeMode === 'future') return;        showTimeTransition('future');    setTimeout(() => {        currentTimeMode = 'future';        const body = document.body;                body.classList.remove('nineties-mode');        removeNinetiesElements();                body.classList.add('future-mode');                addFutureGrid();                addDataStream();                addFutureParticles();                transformCardsToFuture();                transformOwlToRobot();                updateTimeButtons('future');                playFutureSound();    }, 500);}function returnToPresent() {    if (currentTimeMode === 'present') return;        showTimeTransition('present');    setTimeout(() => {        currentTimeMode = 'present';        const body = document.body;                body.classList.remove('nineties-mode', 'future-mode');        removeNinetiesElements();        removeFutureElements();                resetCards();                resetOwl();                updateTimeButtons('present');    }, 500);}function showTimeTransition(mode) {        let transition = document.querySelector('.time-transition');    if (!transition) {        transition = document.createElement('div');        transition.className = 'time-transition';        document.body.appendChild(transition);    }        transition.className = 'time-transition';    if (mode === 'past') transition.classList.add('to-past');    else if (mode === 'future') transition.classList.add('to-future');        setTimeout(() => transition.classList.add('active'), 10);        setTimeout(() => {        transition.classList.remove('active');    }, 1000);        createTimeWarpEffect();}function createTimeWarpEffect() {    const warp = document.createElement('div');    warp.className = 'time-warp-effect active';    document.body.appendChild(warp);    setTimeout(() => warp.remove(), 1500);}function addCRTEffect() {    if (document.querySelector('.nineties-crt')) return;    const crt = document.createElement('div');    crt.className = 'nineties-crt';    document.body.appendChild(crt);}function addWindowsTaskbar() {    if (document.querySelector('.nineties-taskbar')) return;    const taskbar = document.createElement('div');    taskbar.className = 'nineties-taskbar';    taskbar.innerHTML = `        <button class="nineties-start-btn">            <span style="font-size: 20px;">🪟</span>            ابدأ        </button>        <div class="nineties-clock" id="ninetiesClock">12:00 PM</div>    `;    document.body.appendChild(taskbar);        updateNinetiesClock();    setInterval(updateNinetiesClock, 1000);}function updateNinetiesClock() {    const clock = document.getElementById('ninetiesClock');    if (clock) {        const now = new Date();        clock.textContent = now.toLocaleTimeString('ar-MA', {            hour: '2-digit',            minute: '2-digit'        });    }}function transformCardsToNineties() {    document.querySelectorAll('.class-card').forEach((card, index) => {        const classData = SiteData.classes[index];        card.classList.add('nineties-window');        card.innerHTML = `            <div class="nineties-title-bar">                <span>${classData.name}.exe</span>                <div class="nineties-title-bar-buttons">                    <button class="nineties-btn">_</button>                    <button class="nineties-btn">□</button>                    <button class="nineties-btn">×</button>                </div>            </div>            <div style="padding: 20px; background: #c0c0c0; color: black; height: calc(100% - 30px); display: flex; flex-direction: column; justify-content: center; align-items: center;">                <div style="font-size: 4rem; margin-bottom: 15px;">💾</div>                <h3 style="margin: 10px 0; font-family: 'MS Sans Serif', sans-serif; font-size: 1.2rem;">${classData.name}</h3>                <p style="margin: 10px 0; font-family: 'MS Sans Serif', sans-serif; font-size: 0.9rem; color: #333;">${classData.description}</p>                <p style="font-family: 'MS Sans Serif', sans-serif; font-size: 0.8rem; color: #666;">عدد الطلاب: ${classData.studentCount}</p>                <button class="nineties-button" style="margin-top: 20px; padding: 8px 20px;">                    افتح المجلد                </button>            </div>        `;    });}function transformOwlToMouse() {    const owl = document.getElementById('owlMascot');    if (!owl) return;    owl.innerHTML = `        <div class="owl-body" style="background: #808080; border-radius: 45% 45% 40% 40%; border: 3px solid #333; box-shadow: inset 0 -5px 10px rgba(0,0,0,0.3);">            <div style="position: absolute; top: 20%; left: 50%; transform: translateX(-50%); display: flex; gap: 2px;">                <div style="width: 35px; height: 25px; background: #a0a0a0; border: 2px solid #666; border-radius: 3px 3px 0 0;"></div>                <div style="width: 35px; height: 25px; background: #a0a0a0; border: 2px solid #666; border-radius: 3px 3px 0 0;"></div>            </div>            <div style="position: absolute; top: 50%; left: 50%; transform: translateX(-50%); width: 20px; height: 20px; background: #606060; border-radius: 50%; border: 2px solid #444;"></div>            <div style="position: absolute; bottom: -15px; left: 50%; transform: translateX(-50%); width: 3px; height: 30px; background: #333;"></div>        </div>        <div class="owl-speech-bubble" style="font-family: 'MS Sans Serif', sans-serif; font-size: 11px;">            <p>جاري الاتصال بالإنترنت...</p>        </div>    `;}function removeNinetiesElements() {    document.querySelectorAll('.nineties-crt, .nineties-taskbar').forEach(el => el.remove());}function addFutureGrid() {    if (document.querySelector('.future-grid')) return;    const grid = document.createElement('div');    grid.className = 'future-grid';    document.body.appendChild(grid);}function addDataStream() {    if (document.querySelector('.future-data-stream')) return;    const stream = document.createElement('div');    stream.className = 'future-data-stream';    document.body.appendChild(stream);}function addFutureParticles() {    if (document.querySelector('.future-particles')) return;    const container = document.createElement('div');    container.className = 'future-particles';    for (let i = 0; i < 30; i++) {        const particle = document.createElement('div');        particle.className = 'future-particle';        particle.style.left = `${Math.random() * 100}%`;        particle.style.animationDelay = `${Math.random() * 5}s`;        particle.style.animationDuration = `${3 + Math.random() * 4}s`;        container.appendChild(particle);    }    document.body.appendChild(container);}function transformCardsToFuture() {    document.querySelectorAll('.class-card').forEach((card, index) => {        const classData = SiteData.classes[index];        card.classList.add('hologram-effect');        card.innerHTML = `            <div class="card-glow" style="background: linear-gradient(90deg, #00ffff, #ff00ff);"></div>            <div class="card-content">                <div class="robot-float" style="font-size: 4rem; margin-bottom: 15px;">🤖</div>                <h3 class="class-title" style="color: #00ffff; text-shadow: 0 0 20px rgba(0,255,255,0.8);">${classData.name}</h3>                <p class="class-motto" style="color: #00ff00;">${classData.motto}</p>                <div class="future-stats">                    <div class="future-stat">                        <span class="future-stat-value">${classData.studentCount}</span>                        <span class="future-stat-label">طالب</span>                    </div>                    <div class="future-stat">                        <span class="future-stat-value">${classData.totalPoints}</span>                        <span class="future-stat-label">نقطة</span>                    </div>                </div>                <button class="explore-btn" style="background: linear-gradient(135deg, #00ffff, #ff00ff); margin-top: 20px;">                    <i class="fas fa-rocket"></i>                    <span>ابدأ الرحلة</span>                </button>            </div>        `;    });}function transformOwlToRobot() {    const owl = document.getElementById('owlMascot');    if (!owl) return;    owl.innerHTML = `        <div class="owl-body robot-float" style="            background: linear-gradient(135deg, #1a1a2e, #2d2d44);            border-radius: 50%;            border: 3px solid #00ffff;            box-shadow: 0 0 30px rgba(0,255,255,0.5), inset 0 0 20px rgba(0,255,255,0.2);        ">            <div style="position: absolute; top: 25%; left: 50%; transform: translateX(-50%); display: flex; gap: 20px;">                <div style="                    width: 20px; height: 20px;                    background: #00ffff;                    border-radius: 50%;                    box-shadow: 0 0 15px #00ffff, 0 0 30px #00ffff;                    animation: pulse 1s infinite;                "></div>                <div style="                    width: 20px; height: 20px;                    background: #00ffff;                    border-radius: 50%;                    box-shadow: 0 0 15px #00ffff, 0 0 30px #00ffff;                    animation: pulse 1s infinite 0.5s;                "></div>            </div>            <div style="                position: absolute;                top: 55%;                left: 50%;                transform: translateX(-50%);                width: 40px;                height: 8px;                background: linear-gradient(90deg, #00ffff, #ff00ff);                border-radius: 4px;                box-shadow: 0 0 10px #ff00ff;            "></div>            <div style="                position: absolute;                bottom: 15%;                left: 50%;                transform: translateX(-50%);                font-size: 0.6rem;                color: #00ffff;                font-family: monospace;            ">AI-2050</div>        </div>        <div class="owl-speech-bubble" style="background: rgba(0,0,0,0.9); border: 1px solid #00ffff; color: #00ffff;">            <p>مرحباً، أنا مساعدك الذكي! 🤖</p>        </div>    `;}function removeFutureElements() {    document.querySelectorAll('.future-grid, .future-data-stream, .future-particles').forEach(el => el.remove());}function resetCards() {    document.querySelectorAll('.class-card').forEach((card, index) => {        const classData = SiteData.classes[index];        card.classList.remove('nineties-window', 'hologram-effect');        card.innerHTML = `            <div class="card-glow"></div>            <div class="card-content">                <div class="class-icon">                    <i class="${classData.icon}"></i>                </div>                <h3 class="class-title">${classData.name}</h3>                <p class="class-motto">"${classData.motto}"</p>                <div class="class-stats">                    <div class="stat">                        <i class="fas fa-user-graduate"></i>                        <span>${classData.studentCount} طالب</span>                    </div>                    <div class="stat">                        <i class="fas fa-star"></i>                        <span>${classData.totalPoints} نقطة</span>                    </div>                </div>                <div class="class-badges">                    ${classData.badges.map(b => `<span class="badge">${b}</span>`).join('')}                </div>                <button class="explore-btn">                    <span>استكشف الفصل</span>                    <i class="fas fa-arrow-left"></i>                </button>            </div>        `;    });}function resetOwl() {    const owl = document.getElementById('owlMascot');    if (!owl) return;    owl.innerHTML = `        <div class="owl-body">            <div class="owl-face">                <div class="owl-eyes">                    <div class="owl-eye left">                        <div class="owl-pupil"></div>                    </div>                    <div class="owl-eye right">                        <div class="owl-pupil"></div>                    </div>                </div>                <div class="owl-beak"></div>            </div>            <div class="owl-wings">                <div class="owl-wing left"></div>                <div class="owl-wing right"></div>            </div>            <div class="owl-feet">                <div class="owl-foot"></div>                <div class="owl-foot"></div>            </div>        </div>        <div class="owl-speech-bubble" id="owlSpeech">            <p>مرحباً! اضغط علي لمفاجأة! 💥</p>        </div>    `;        initOwlEvents();}function updateTimeButtons(mode) {    document.querySelectorAll('.time-btn').forEach(btn => {        btn.classList.remove('active');    });    const activeBtn = document.querySelector(`.time-btn.${mode}-btn`);    if (activeBtn) activeBtn.classList.add('active');}function playDialUpSound() {    try {        const playTone = (frequency, duration, delay) => {            setTimeout(() => {                const oscillator = dialUpAudioContext.createOscillator();                const gainNode = dialUpAudioContext.createGain();                oscillator.connect(gainNode);                gainNode.connect(dialUpAudioContext.destination);                oscillator.frequency.value = frequency;                oscillator.type = 'sine';                gainNode.gain.setValueAtTime(0.2, dialUpAudioContext.currentTime);                gainNode.gain.exponentialRampToValueAtTime(0.01, dialUpAudioContext.currentTime + duration);                oscillator.start(dialUpAudioContext.currentTime);                oscillator.stop(dialUpAudioContext.currentTime + duration);            }, delay);        };                playTone(350, 0.1, 0);        playTone(440, 0.1, 150);        playTone(350, 0.1, 300);        playTone(440, 0.15, 450);        playTone(480, 0.2, 600);        playTone(350, 0.1, 800);        playTone(440, 0.3, 950);    } catch (e) {        console.log('Audio not supported');    }}function playFutureSound() {    try {        const oscillator = dialUpAudioContext.createOscillator();        const gainNode = dialUpAudioContext.createGain();        oscillator.connect(gainNode);        gainNode.connect(dialUpAudioContext.destination);        oscillator.type = 'sine';        oscillator.frequency.setValueAtTime(200, dialUpAudioContext.currentTime);        oscillator.frequency.exponentialRampToValueAtTime(1000, dialUpAudioContext.currentTime + 0.5);        oscillator.frequency.exponentialRampToValueAtTime(400, dialUpAudioContext.currentTime + 1);        gainNode.gain.setValueAtTime(0.2, dialUpAudioContext.currentTime);        gainNode.gain.exponentialRampToValueAtTime(0.01, dialUpAudioContext.currentTime + 1.2);        oscillator.start(dialUpAudioContext.currentTime);        oscillator.stop(dialUpAudioContext.currentTime + 1.2);    } catch (e) {        console.log('Audio not supported');    }}window.travelToPast = travelToPast;window.travelToFuture = travelToFuture;window.returnToPresent = returnToPresent;